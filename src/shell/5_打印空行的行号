#!/bin/bash

awk '/^\s*$/{print NR}' nowcoder.txt


awk中 /正则表达式/表示匹配模式
基本正则表达式(BRE)
常用符号
. : 匹配任意单个字符(除换行符外)
^ : 匹配行的开头
$ : 匹配行的结尾
* : 匹配前面的字符多次或者零次
[] : 匹配字符类中的任何一个字符
\ : 转义符,用于转义特殊字符 (如 * , [ , $等)

扩展正则表达式(ERE)
+ : 匹配前一个字符一次或多次
? : 匹配前一个字符0次或1次
() : 分组,用于创建子模式
{n,m} : 匹配前一个字符至少n次,最多m次

\s用来匹配任何一个空白字符