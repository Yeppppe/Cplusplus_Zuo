#!/bin/bash

grep -v '^$' nowcoder.txt

-v表示反向匹配,也就是不匹配



##第二次写法
awk '{if($0 != ""){print $0}}' nowcoder.txt

当 awk 读取一行文本时，它会将整个行存储在一个内置的变量中。这个变量就是 $0。其他字段（$1, $2 等）是基于行内容被拆分后的结果，它们表示的是按分隔符拆分后的各个部分。

$0 包含了 整行文本，无论这一行有多少字段。

$1, $2, $3 等 是对 当前行 根据字段分隔符拆分后的部分的引用。比如，如果输入行有多个单词，$1 就是第一个单词，$2 是第二个单词，以此类推。